<nav class="con_nav">  
	<%= render partial: "shared/navbar" %>
</nav>

<%= render partial: "shared/flash" %>

<div class= "container">
	<h1 class="header">Geek Events</h1>
	<br>
	<table class="events_list table table-bordered table-responsive table-condensed table-hover">
		<thead>
		<tr>
			<th>Date</th>
			<th style="display: none">Timesort</th>
			<th>Time</th>
			<th>Name</th>
			<th>Price</th>
			<th>Neighborhood</th>
			<th>More Info</th>
		</tr>
		</thead>
		<tbody>
		<% @cons.each do |con| %>
		<tr>
			<td><%= con.event_date.strftime("%m/%d/%y") %></td>
			<td style="display: none"><%= con.start_time %></td>
			<td><%= con.start_time.strftime("%l:%M%P") %></td>
			<td><strong><%= con.name %></strong></td>
			<td><%= con.price %></td>
			<td><% if con.neighborhood %><%= con.neighborhood.name %><% end %></td>
			<td><%= link_to 'Details', con %></td>
		</tr>
		<% end %>
		</tbody>
	</table>

	<%= link_to 'New Geek Event', new_con_path %>
</div>